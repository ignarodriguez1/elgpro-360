import { OrderDetailSkeleton } from "@/components/shared/RouteSkeletons";

// Skeleton con la forma real de la OT: hero + barra de etapas + timeline + sidebar.
export default function Loading() {
  return <OrderDetailSkeleton />;
}
