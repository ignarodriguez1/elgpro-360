import { CustomerTrackingSkeleton } from "@/components/shared/RouteSkeletons";

// Cubre el detalle del vehículo y sus rutas anidadas (orden / historial).
export default function Loading() {
  return <CustomerTrackingSkeleton />;
}
